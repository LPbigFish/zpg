# GLAD

This is a local target placeholder. Generate an OpenGL 4.6 Core GLAD loader
with `GLFW_INCLUDE_NONE`, then add generated `include/` and `src/glad.c` files
and replace this interface target with a small static library.
